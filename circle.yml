general:
  branches:
    only:
      - master
machine:
  services:
    - docker
checkout:
  post:
    - git clone git@github.com:jcouyang/blog.git -b gh-pages public
    - ./preserve_timestamp.sh
test:
  post:
    - ./ci/compile
    - ./ci-publish.sh
